# 仓库重建说明（2026-09-12）

原仓库 `E:\MOD包\ksp2` 在磁盘整理中丢失（git 历史、NBodyMJ 未提交改动、PartFailures/EngineLifecycle/LoadBoost 源码均不可恢复；游戏中已部署的这些插件 dll 仍在运行）。

本仓库（`E:\仓库`）按会话记录重建，完整恢复了 **CrewHiring** 项目：

- 全部源码（含终审修复后的最终版：P2/P3 用 Harmony finalizer 恢复）
- 21 个 xunit 单测（重建后验证 21/21 通过，Release 构建 0 错误）
- `GameData/CrewHiring/Kolonists.cfg`、`tools/deploy-ch.sh`（默认部署目标已改为现主安装 `E:\ksp\Kerbal Space Program`）
- `refs/`：harmony/0Harmony.dll、kerbalism/Kerbalism.dll（3.42，取自主安装）、mks/AC 源码参考副本（GPL）
- 设计文档 `docs/superpowers/specs/2026-08-25-crewhiring-design.md`

部署状态：2026-09-19 主安装迁至 `E:\SteamLibrary\steamapps\common\Kerbal Space Program`（旧 `E:\ksp\...` 已删除），用仓库内既有 Release 二进制重新部署后，**按用户要求仅保留 LoadBoost**（`GameData\LoadBoost\`：LoadBoost.dll + LoadBoost.Core.dll + LoadBoostSettings.txt）；CrewHiring、EngineLifecycle 已从游戏 GameData 删除（源码与部署脚本仍在仓库中，需要时跑 `tools/deploy-ch.sh` / `deploy-el.sh` 即可恢复）。注意：本机当前仅有 .NET 7 运行时、无 .NET SDK，暂时无法重新编译，如需改代码需先装 SDK（项目目标框架 net48/net481）。

待办（会话中未关闭事项）：
- CrewHiring 游戏内手动验收清单（界面渲染/雇佣扣款/设置页/读档保持/Medic 数值生效）。
- KerbalismTweaks（Settings.cfg 游戏内开关）仅有设计草案（数值即时生效 + 功能开关热切换含边界提示），未实施；如需继续，按 2026-08-29 会话中确认的 spec 重新立项。
- 主包发射台"带辐射 1403.977 rad/h"异常显示待查（疑似 Kopernicus/Principia 与 Kerbalism 辐射几何交互；配置层面无解释）。
